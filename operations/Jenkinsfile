#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('Configure Directories') {
            steps {
                sh """pwd
                ls -lart
                
                """
            }
        }
        stage('Drupal Docker Image') {
            steps {
                sh """pwd
                sleep 5s
                """
            }
        }
    }
}