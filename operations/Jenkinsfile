#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('Updated Staging Instance') {
            steps {
                sh """pwd
                ssh -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -i /var/lib/jenkins/.ssh/common-key-mumbai.pem -t ubuntu@13.234.148.202 sudo sh /tmp/extract.sh
                """
            }
        }
    }
}
